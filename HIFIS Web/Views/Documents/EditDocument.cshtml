@model DocumentViewModel 
@{
    ViewBag.ViewTitle = Labels.viewTitle_EditDocument;
    
    ViewBag.LayoutView = LayoutPage.Client;
}

@Html.Partial("_ValidationSummaryOuter")

<div>
    @using (Html.BeginForm("EditDocument", "Documents", FormMethod.Post, new { @class = "form-horizontal" }))
    {  
        @Html.Partial("_Document", Model);
                
        <div class="form-group">
			<div class="col-sm-offset-2 col-sm-10">
				<button type="submit" class="saveButton  ">@Labels.Save</button>
                @Html.ActionLink(Labels.Cancel, "DocumentList", "Documents", new { id = Model.ClientID }, new { @class = "cancelButton", alt=Labels.Cancel, aria_label = Labels.Cancel, @role = "button" })
			</div>
		</div>                           
    }
</div>

<div>@Html.Partial("_BaseFooter", (BaseViewModel)Model)</div>
